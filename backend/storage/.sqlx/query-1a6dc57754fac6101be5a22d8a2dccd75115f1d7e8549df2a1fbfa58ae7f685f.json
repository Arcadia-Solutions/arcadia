{
  "db_name": "PostgreSQL",
  "query": "\n            SELECT\n                COUNT(c.id)\n            FROM\n                conversations c\n            JOIN LATERAL (\n                SELECT\n                    cm.created_at,\n                    cm.created_by_id\n                FROM\n                    conversation_messages cm\n                WHERE\n                    cm.conversation_id = c.id\n                ORDER BY\n                    cm.created_at DESC\n                LIMIT 1\n            ) AS lm ON TRUE\n            WHERE\n                lm.created_by_id != $1\n                AND\n                (\n                    (c.sender_id = $1 AND (c.sender_last_seen_at < lm.created_at))\n                    OR\n                    (c.receiver_id = $1 AND (c.receiver_last_seen_at IS NULL OR c.receiver_last_seen_at < lm.created_at))\n                )\n                AND NOT (c.sender_id = $1 AND c.deleted_by_sender)\n                AND NOT (c.receiver_id = $1 AND c.deleted_by_receiver)\n            ",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "count",
        "type_info": "Int8",
        "origin": "Expression"
      }
    ],
    "parameters": {
      "Left": [
        "Int4"
      ]
    },
    "nullable": [
      null
    ]
  },
  "hash": "1a6dc57754fac6101be5a22d8a2dccd75115f1d7e8549df2a1fbfa58ae7f685f"
}
