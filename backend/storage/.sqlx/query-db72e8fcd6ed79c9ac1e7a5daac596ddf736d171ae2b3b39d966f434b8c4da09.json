{
  "db_name": "PostgreSQL",
  "query": "\n            WITH periods AS (\n                SELECT generate_series(\n                    date_trunc($3, $1::DATE::TIMESTAMP),\n                    date_trunc($3, $2::DATE::TIMESTAMP),\n                    ('1 ' || $3)::INTERVAL\n                ) AS period\n            ),\n            stats AS (\n                SELECT\n                    date_trunc($3, u.created_at)::TIMESTAMP AS period,\n                    COUNT(*)::BIGINT AS count\n                FROM users u\n                WHERE u.created_at >= $1::DATE\n                  AND u.created_at < ($2::DATE + INTERVAL '1 day')\n                GROUP BY period\n            )\n            SELECT\n                p.period::TIMESTAMP AS \"period!\",\n                COALESCE(s.count, 0)::BIGINT AS \"count!\"\n            FROM periods p\n            LEFT JOIN stats s ON p.period = s.period\n            WHERE p.period >= COALESCE(\n                date_trunc($3, (SELECT MIN(created_at) FROM users))::TIMESTAMP,\n                p.period\n            )\n            ORDER BY p.period\n            ",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "period!",
        "type_info": "Timestamp",
        "origin": "Expression"
      },
      {
        "ordinal": 1,
        "name": "count!",
        "type_info": "Int8",
        "origin": "Expression"
      }
    ],
    "parameters": {
      "Left": [
        "Date",
        "Date",
        "Text"
      ]
    },
    "nullable": [
      null,
      null
    ]
  },
  "hash": "db72e8fcd6ed79c9ac1e7a5daac596ddf736d171ae2b3b39d966f434b8c4da09"
}
