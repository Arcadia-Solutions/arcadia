{
  "db_name": "PostgreSQL",
  "query": "\n            SELECT COUNT(*)\n            FROM conversations AS c\n            JOIN users AS co ON (CASE WHEN c.sender_id = $1 THEN c.receiver_id ELSE c.sender_id END) = co.id\n            WHERE\n                (c.sender_id = $1 OR c.receiver_id = $1)\n                AND (\n                    $2::TEXT IS NULL\n                    OR c.subject ILIKE '%' || $2 || '%'\n                    OR co.username ILIKE '%' || $2 || '%'\n                    OR (NOT $3 AND EXISTS (\n                        SELECT 1 FROM conversation_messages cm\n                        WHERE cm.conversation_id = c.id\n                        AND cm.content ILIKE '%' || $2 || '%'\n                    ))\n                )\n            ",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "count",
        "type_info": "Int8"
      }
    ],
    "parameters": {
      "Left": [
        "Int4",
        "Text",
        "Bool"
      ]
    },
    "nullable": [
      null
    ]
  },
  "hash": "6427186f5b39b1e5f1c83d90d791bfb03d629e60846fe24ce00a522d0e4ccdc2"
}
